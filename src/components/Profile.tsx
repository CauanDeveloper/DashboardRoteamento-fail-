import React from 'react';
import styles from '../styles/Profile.module.css';

const Profile: React.FC = () => {
  return <div className={styles.pageContainer}>Profile Page</div>;
};

export default Profile;
